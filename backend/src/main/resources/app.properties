## Added allowPublicKeyRetrieval=true for local development when using
## caching_sha2_password authentication. This is safe for localhost testing
## but DO NOT enable in production without understanding the security implications.
db.url=jdbc:mysql://localhost:3306/auctiondb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=buyme_app
db.pass=StrongP@ssw0rd!
